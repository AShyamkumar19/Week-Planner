package cs3500.pa05.controller.Commands;

/**
 * The Command interface represents a command that can be executed by a controller.
 */
public interface Command {

  /**
   * Executes the command.
   */
  void execute();
}
